name: P0 Patch Runner

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/p0-data-truth-patch.yml

jobs:
  verify-runner:
    runs-on: ubuntu-latest
    steps:
      - name: Verify runner
        run: echo "P0 patch runner ready"
